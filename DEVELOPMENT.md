# 開発の進め方

一人で作るための運用メモ。重い仕組みは入れず、**事故が起きる場所にだけ**手を打つ。

## 前提

- **一人で作る。** レビュアーも承認者もいないので、PR やブランチ保護は入れない。
- **`main` は公開されている。** public リポジトリで GitHub Pages を配信しているため、
  `main` が壊れる＝公開物が壊れる。ここだけは守る。
- **作業中の速さは落とさない。** ローカルで静的サーバーを立てて直す流れは今までどおり。
  Git と Vercel は「区切りがついたあと」に効く仕組みで、書いている最中には触らない。

## 事故の型と、効く対策

| 事故 | 具体例 | 効くもの |
|---|---|---|
| 失う | Mac が壊れる、消す、戻せない | コミットと push |
| 直前に壊す | 発表前日の修正で動かなくなる | タグ |
| 本番で動かない | フォント、ブラウザ差、デバイス認識 | 本番チェックリスト、実機リハ |
| 機材が壊れる | SD 破損、ケーブル断線 | 物理の予備（Git では守れない） |

## ブランチ

| ブランチ | 役割 |
|---|---|
| `main` | 動く状態だけ。Pages がここを配信する |
| `work/<作品名>` | 制作中。作品ごとに1本。例 `work/dancetoggle` |

分ける理由は、`main` が公開されているから。発表や展示の最中に `main` を直接触ると、
その瞬間に公開物が壊れる。作業を別の枝に隔離しておけば、`main` はいつでも見せられる状態で残る。

### 基本の流れ

```bash
git switch -c work/dancetoggle
# ... 作る。区切りでコミット
git switch main
git merge --no-ff work/dancetoggle
git push
git branch -d work/dancetoggle
```

`--no-ff` を付けるのは、**その作品の作業がどこからどこまでか**を履歴に残すため。
あとで「何を試して何を捨てたか」を書くときに、この区切りが材料になる。

### ブランチを切らなくていい場面

`experiments/` の技術検証と、README や本ドキュメントの更新は `main` 直でよい。
公開物ではないので壊れても困らないし、**切るかどうかを毎回考える方が損**。

迷ったら「これは公開ページに出るか？」だけで決める。出るなら枝、出ないなら直。

## コミット

- 区切りでコミットし、**その日のうちに push** する。1日1回で十分。
- メッセージは日本語の短い名詞句でよい。規約は作らない。
  - 例：`DanceToggle: 二人モードの溜め判定`
- 粒度は気にしない。完璧な履歴より、**手元にしかない状態を作らない**ことが目的。

## タグ — 動いた版に印を打つ

「あの日は動いていたのに」を防ぐ唯一の手段。年3回でいい。

```bash
git tag mid-2026-09-24
git push --tags
```

| タイミング | タグ |
|---|---|
| 中間発表 2026-09-24 | `mid-2026-09-24` |
| シミュレーション審査 2026-11-05 | `sim-2026-11-05` |
| 搬入 2026-11-26 | `exhibit-2026-11-26` |

**展示や発表の直前に必ず打つ。** 会場で `main` を直接直すことになっても、
打ってさえあれば動いていた状態に戻れる。

## 壊したときの戻し方

| やりたいこと | コマンド |
|---|---|
| 未コミットの変更を全部捨てる | `git restore .` |
| 1ファイルだけ元に戻す | `git restore <path>` |
| コミットは取り消すが変更は残す | `git reset --soft HEAD^` |
| タグの状態を見にいく | `git switch --detach mid-2026-09-24` |
| そこから元に戻る | `git switch main` |

## 本番チェックリスト

各作品の README の末尾に置き、**搬入日の朝に上から読む**。
事故の大半は「知らなかった」ではなく「確認し忘れた」で起きる。

共通項目：

```markdown
## 本番チェックリスト
- [ ] フォントが表示されるか（豆腐が出ていないか）
- [ ] 想定する解像度で崩れないか
- [ ] 入力デバイスが認識されるか（ボタン番号を実機で実測し直す）
- [ ] 30分放置して画面が消えないか
- [ ] 電源を入れ直して自動で起動するか
- [ ] 予備：SDカード / ケーブル / 変換アダプタ
```

作品固有の項目（例：DanceToggle なら 2台接続時のスロット順、`swapped` の要否）は
各 README 側に足す。

### 環境差でよく踏むところ

- **フォント**：ローカルの Mac にしか無いフォントは、他の環境で別物になる。
  配信・展示するものは `@font-face` でリポジトリに同梱する。
- **Gamepad API はセキュアコンテキスト（HTTPS / localhost）でしか動かない。**
  `http://192.168.x.x` のような LAN 越しの平文 HTTP では**パッドだけ反応しない**。
- **ボタン番号は OS とブラウザで変わる。** macOS で実測した値が Linux で同じとは限らない。
  マッピングは `config.js` のような1ファイルに集めておき、実機で測り直して差し替える。
- **絶対パス**は配信先のサブパスで壊れる。`/src/...` ではなく相対パスで書く。

## public リポジトリなので

- **入れないもの**：有償素材、他人が写った映像、被験者データ、`.env`
- フォントを同梱するときは、ライセンス文（OFL 系なら `OFL.txt`）も一緒に置く
- 2026-09-20 時点で、リポジトリにメディアファイル（画像・動画・音声）は1件も無い。
  この状態を保てるうちは権利面を考えなくて済む

## 入れないもの

時間を食うわりに事故を減らさないので、意識的に捨てる。

- ブランチ保護 / PR 必須 — 一人なので承認者がいない
- CI / 自動テスト — 作品に単体テストを書く意味が薄い
- コミットメッセージ規約（Conventional Commits 等） — 読むのは自分だけ
- Docker / TypeScript 移行 / モノレポツール — オーバーキル
- Issue・プロジェクト管理 — 制作ノートは Obsidian にあるので二重管理になる

## 機材（Git では守れない領域）

- **SD カードは2枚**用意し、動く状態をクローンしておく。展示中に壊れても差し替えで1分。
- **型番・VID/PID・実測値は作品の README に残す。**
  例：`works/DanceToggle/usb-dance-mat-knowledge.md`
- 予備に持っていく：ケーブル、変換アダプタ、マイコン、電源アダプタ
